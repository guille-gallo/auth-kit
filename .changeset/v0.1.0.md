---
'@guille/auth-kit': minor
---

Initial public release of @guille/auth-kit — a Supabase auth layer for React apps with a typed `useAuth` hook, `AuthScreen`, `AuthGate`, and `ProtectedRoute` components.
